services:
  web:
    image: nginx
    ports:
      - "8080:80"
    depends_on:
      - db
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: labpassword
    volumes:
      - pgdata:/var/bin/postgresql/data
volumes:
  pgdata:
