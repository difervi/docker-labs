services:
  web:
    build: .
    image: lab-compose-web:latest
    ports:
      - "8080:80"
    depends_on:
      - db
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/bin/postgresql/data
volumes:
  pgdata:
